module.exports = (baseUrl) => (req) => {  const parsedUrl = new URL(req.url, baseUrl)  const params = {}  parsedUrl.searchParams.forEach((value, key) => params[key] = value)  req.pathnameParts = parsedUrl.pathname.match(/\/\w+/g)  req.params = params}